#!/usr/bin/env bash
COUNT="$HOME/.garden/presence/meta/count.txt"
VIEW="$HOME/.garden/lantern/views/meta.txt"

{
  echo "Meta Engine"
  echo "==========="
  echo ""
  echo "Total Updates:"
  cat "$COUNT"
  echo ""
} > "$VIEW"

garden page "Meta" "$VIEW"
